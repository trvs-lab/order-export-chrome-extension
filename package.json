{
  "name": "order-export-chrome-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Local Chrome extension for exporting visible JD order list data to CSV.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
